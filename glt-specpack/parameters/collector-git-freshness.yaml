apiVersion: glt.dev/parameters/v1
kind: ParameterCard
metadata:
  id: glt.param.collector.git.freshness_ttl_seconds
  revision: 1
  owner: reliability
spec:
  default: 3600
  range: { min: 300, max: 86400 }
  unit: seconds
  risk_if_low: Git facts treated stale while the tree is still the pin
  risk_if_high: Stale git facts used as current materialized evidence
  calibration: Same window as snapshot P01 until DEV-17 measures collector age
  spec_ref: docs/SPEC/collectors.md
