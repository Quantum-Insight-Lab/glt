apiVersion: glt.dev/parameters/v1
kind: ParameterCard
metadata:
  id: glt.param.snapshot.stale_after_seconds
  revision: 1
  owner: reliability
spec:
  default: 3600
  range: { min: 300, max: 86400 }
  unit: seconds
  risk_if_low: Stale topology acted upon
  risk_if_high: Excessive freshness churn and noise
  calibration: Post-incident review of snapshot age at decision time
  spec_ref: docs/SPEC/snapshots.md
